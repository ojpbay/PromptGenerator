<Window x:Class="PromptContextGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PromptContextGenerator"
        mc:Ignorable="d"
        Title="Prompt Context Generator" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="userInput" 
                     Grid.Column="0"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Margin="0,0,10,0"
                     Text="I am going to Paris, what should I see?"/>
            
            <Button x:Name="GenerateButton"
                    Content="Generate" 
                    Grid.Column="1"
                    Width="100" 
                    Height="30" 
                    Click="GenerateButton_Click" />
        </Grid>
        
        <TextBox x:Name="results"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 Background="#F8F8F8"/>
    </Grid>
</Window>